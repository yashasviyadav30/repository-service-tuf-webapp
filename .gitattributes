# Generated by `uv lock`, not written by hand. Marked so review tools
# collapse it instead of showing a wall of hashes.
backend/uv.lock linguist-generated=true

# A shell script checked out with CRLF is not runnable: the interpreter
# named on the first line comes back with a carriage return attached.
*.sh text eol=lf
