# Mock mode is the default until Yashasvi's backend branch lands.
# Flip to false (and set VITE_API_BASE_URL) per-page as each endpoint
# becomes available -- see src/pages/*/endpoints/*.ts.
VITE_USE_MOCKS=true
VITE_API_BASE_URL=/api
