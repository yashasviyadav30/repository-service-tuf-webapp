apiVersion: v1
kind: Service
metadata:
  name: {{ include "rstuf-webapp.fullname" . }}
  labels:
    {{- include "rstuf-webapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rstuf-webapp.selectorLabels" . | nindent 4 }}
